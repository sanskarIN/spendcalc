# SpendCalc core functionality does not require environment secrets or remote API keys.
# Keep this file as documentation if future optional integrations introduce configuration.
# Example placeholders must use non-secret values only.
