# SpendCalc — Latest Work Continuity

This file is retained only for compatibility with earlier handoffs.

The canonical, current engineering record is [`what_changed.md`](what_changed.md).

The active application release target is `2.0.12`; exact branch-head, workflow, manual-verification, signing, and release truth must be read from the canonical root handoff and [`docs/verification.md`](docs/verification.md).

Do not use the older PR `#1`, `verify/release-candidate`, or historical `1.0.0` details from earlier handoffs as the current continuation point. The active completion/verification path is PR `#12` on `complete/v1-finalization` while that PR remains open, and it must be checked against the current GitHub pull-request/workflow state before further changes.
